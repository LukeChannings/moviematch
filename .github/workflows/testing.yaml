name: Testing

on:
  push:
    branches: [ '*' ]
  pull_request:
    branches: [ '*' ]

jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:

    - uses: denolib/setup-deno@v2
      with:
        deno-version: v1.6.3
    - run: |
        chmod +x ./scripts/test.sh
        ./scripts/test.sh
